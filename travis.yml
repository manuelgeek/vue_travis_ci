#if: branch = dev
language: node_js
node_js:
  - 12

cache:
  npm: true
  directories:
    - ./node_modules

before_script:
  - npm install

script:
  - npm run build

test:
  provider: script
  skip_cleanup: true
  script:
    npm run test
  branches:
    only:
      - dev

deploy:
  provider: script
  skip_cleanup: true
  script:
    echo "I am true"
  on:
    branch: master
